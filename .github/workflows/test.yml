on: issues

permissions: {}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: check gates
      id: branch-deploy
      uses: jarlob/branch-deploy@gate
      
    - name: print title
      env:
        TITLE: ${{ github.event.issue.title }}
      run: echo "$TITLE"
